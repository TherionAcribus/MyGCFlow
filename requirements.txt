# Dépendances de l'application (exécution et empaquetage).
Flask==3.1.3
Flask-Babel==4.0.0
Flask-Compress==1.24
Flask-SQLAlchemy==3.1.1
Pillow==11.3.0
# Fournit le binaire ffmpeg utilisé pour les deux pipelines vidéo (assemblage
# d'images et post-traitement MediaRecorder).
imageio-ffmpeg==0.6.0
requests==2.34.2
# Comparaison des numéros de version (vérification des mises à jour).
packaging==26.3
# Serveur de production (launcher.py) et icône de la zone de notification.
waitress==3.0.2
pystray==0.19.5
